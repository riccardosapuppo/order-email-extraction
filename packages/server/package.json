{
  "name": "@order-email/server",
  "version": "1.0.0",
  "private": true,
  "description": "The orders, and the emails they were read out of. No database: the mail is the record.",
  "type": "module",
  "main": "./build/src/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node build/src/main.js",
    "test": "tsc -p tsconfig.json && cd build && node --test"
  },
  "dependencies": {
    "@order-email/core": "1.0.0",
    "express": "4.22.2"
  }
}
