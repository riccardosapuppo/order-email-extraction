{
  "name": "order-email-extraction",
  "private": true,
  "version": "1.0.0",
  "description": "Reads a mailbox and rebuilds the life of an order from it, saying where every field came from.",
  "author": "Riccardo Sapuppo",
  "license": "MIT",
  "workspaces": [
    "packages/core",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "start": "npm run build && node tools/run-all.mjs",
    "mailbox": "node mail-server/imap.mjs",
    "server": "node packages/server/build/src/main.js",
    "web": "npm start -w @order-email/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "extract": "node tools/extract.mjs",
    "check:screen": "node tools/through-the-screen.mjs",
    "check:mark": "node tools/check-mark.mjs",
    "screenshots": "node tools/screenshots.mjs"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "typescript": "5.6.3",
    "playwright-core": "^1.48.0"
  },
  "engines": {
    "node": ">=20.11"
  }
}
